<UserControl x:Class="CaliburnMicroPlaytime.ColorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="Azure">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <RadioButton Name="Red" Content="Red" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <RadioButton Name="Green" Content="Green" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" />
        <RadioButton Name="Blue" Content="Blue" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="2" />
    </Grid>
</UserControl>
